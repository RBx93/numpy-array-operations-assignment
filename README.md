# numpy-array-operations-assignment
This assignment uses NumPy to create and manipulate a 1D array of random uniform values. You will compute mean and standard deviation, reshape the array to 5×1, add 5 to each element, and compute the dot product of the reshaped array with itself, printing results after each step.
